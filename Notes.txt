Application -> business logic (eventlarý kontrol ettiðimiz) layer.
Feature Based Progress -> Projede belirli bölümleri oluþturmak gibi birþey.(Her veritabaný nesnesi ayrý bir feature olarak ele alýnýyor)
Service -> Repositories, Dýs Servisler, 

Domain -> Entity'lerimizi domain katmanýnda tutuyoruz.

Infrastrature -> Baþka sistemlerin projeye entegarasyonu amacýyla kullanýlýyor.

Persistence -> Bir veri kaynaðý ile çalýþmak. Veri tabaný implementasyonlarý, migrationlarýmýzý, repository'leri implemente ettiðimiz yer.

WebAPI -> Apileri oluþturduðumuz kýsým

Persistence Repository Ýmplementasyonu

ServiceRegistration -> Bütün servisler, bütün katmanlar kendi registration'larýný kendi katmanlarýnda yapsýnlar.

public static IServiceCollection AddPersistenceServices(this IServiceCollection services, IConfiguration configuration)
this keywordunu kullandýðýmýz zaman services'ýn bir parametre deðil IServiceCollection nesnesine extention metod yazmak istediðimizi belirtiyoruz.

Application : Dýþ servisleri ve tüm baðýmlýlýklarý bu proje yönetiyor. CQRS burada implemente edilecek

CQRS -> Command,Query,Responsiblity,Segregation -> Update, Delete, Create -> Commandlar
Query -> Select operasyonlarý
